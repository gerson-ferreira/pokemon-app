import { PokemonFilterComponent } from './pokemon-filter.component';

describe('PokemonFilterComponent', () => {
  let component: PokemonFilterComponent;

  beforeEach(() => {
    component = new PokemonFilterComponent();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
